# Vision-Edge-project
YOLO object-detection pipeline: install libs, run basic image detection, export model to ONNX/TensorRT for speed, benchmark FPS &amp; VRAM usage, then process video and run live webcam inference — validating the model for fast, real-time deployment.
